"""FinOps domain — the platform proof.

The SAME engine that heals microservices heals a cloud bill: SENTINEL detects a
spend anomaly, inspects an over-provisioned Kubernetes/cloud inventory, stages a
rightsizing plan, *simulates* the savings and SLA impact (the verification step),
gates on an explainable confidence score, applies the plan, confirms spend
dropped, and files the report.

No engine code changes to add this domain — only this adapter. That is the
"pluggable core" claim, demonstrated rather than asserted.
"""
